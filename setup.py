"""
Minimal setup.py shim for backward compatibility.
All configuration is now in pyproject.toml (PEP 621).
"""

from setuptools import setup

# All metadata and configuration now lives in pyproject.toml
setup()
